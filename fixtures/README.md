# RXScan controlled fixtures

This directory is reserved for deterministic, authorized local test fixtures. Phase 0 establishes the contract: fixtures never target public infrastructure and must state their ground truth, resource limits, and supported phases.

The initial matrix covers target parsing and scope policy. Later phases add IPv4/IPv6, TCP, UDP, HTTP wildcard, redirects, throttling, oversized response, crawl, API, and fuzzing fixtures.
